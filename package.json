{
  "name": "app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:print": "node scripts/check-print-flow.mjs",
    "check:menu": "node scripts/check-menu-update.mjs",
    "test": "npm run check:print && npm run check:menu && npm run build"
  },
  "devDependencies": {
    "@netlify/functions": "^6.0.0",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.1",
    "web-push": "^3.6.7"
  }
}
