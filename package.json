{
  "name": "app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:api": "netlify dev",
    "build": "vite build",
    "preview": "vite preview",
    "check:print": "node scripts/check-print-flow.mjs",
    "check:menu": "node scripts/check-menu-update.mjs",
    "check:release": "node scripts/check-release.mjs",
    "verify:prod": "node scripts/verify-deploy.mjs",
    "test": "npm run check:print && npm run check:menu && npm run check:campaign && npm run build",
    "deploy": "npm test && npm run check:release && netlify deploy --prod --dir dist --functions netlify/functions && npm run verify:prod",
    "deploy:preview": "npm test && netlify deploy --dir dist --functions netlify/functions",
    "check:campaign": "node scripts/check-campaign.mjs"
  },
  "devDependencies": {
    "@netlify/functions": "^6.0.0",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.1",
    "web-push": "^3.6.7"
  }
}
